# Published launcher data

This directory is maintained automatically by the server synchronization
task. It will contain:

```text
steam-servers.json
steam-servers.json.sig
```

Do not edit generated files manually. The signature is created from the exact
JSON bytes and must be regenerated whenever the JSON changes.
